# Experimental Backend Scaffold

This folder is retained for reference only.

The canonical backend is `trading_os/`. Android app integration, API contract
docs, persistence, licensing, runtime supervision, analytics, reports, and final
pre-APK checks should target `trading_os/`.

Do not run two backend architectures as active production targets. Useful modules
from this folder can be migrated into `trading_os/` later after review.
